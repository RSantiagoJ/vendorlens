[pytest]
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated
markers =
    mock: offline tests — no API keys or running services required
    integration: requires ANTHROPIC_API_KEY and ingested ChromaDB
norecursedirs = tests/live
